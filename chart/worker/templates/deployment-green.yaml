{{if .Values.global.green.enabled}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-green
  labels:
    tier: worker
    app: voting-app
spec:
  replicas: 1
  selector:
    matchLabels:
      tier: worker
  template:
    metadata:
      labels:
        tier: worker
        slot: green
    spec:
      containers:
      - image: {{.Values.image}}
        name: worker
{{end}}
